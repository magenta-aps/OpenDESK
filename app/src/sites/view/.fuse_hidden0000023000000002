<div class="od-projects" ng-controller="SitesController as siteCtrl">
    
    <h1 class="od-title">Projekter</h1>

    <button class="od-btn" ng-click="siteCtrl.newSite($event)"><i class="material-icons">create</i> Nyt Projekt</button>
		
		<button class="od-btn" ng-click="siteCtrl.deleteSite($siteName)"><i class="material-icons">delete</i> Slet Projekt</button>
    
    <button class="od-btn disabled"><i class="material-icons">file_upload</i> Upload</button>
    <button class="od-btn disabled"><i class="material-icons">create</i> Nyt dokument</button>
		
    <ul class="od-list">
        <li ng-repeat="content in vm.contents">
            <a href="#/projekter/{{content.name}}?conservedPath={{content.conservedPath}}"><i class="material-icons">work</i> {{content.name}}  ({{content.contentType}})  </a></li>
    </ul>


</div>


